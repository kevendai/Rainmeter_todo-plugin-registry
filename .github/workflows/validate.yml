name: validate-registry
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate registry
        shell: pwsh
        run: ./scripts/Test-Registry.ps1
